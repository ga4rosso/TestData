apiVersion: v1
kind: Pod
metadata:
  labels:
    run: sampleapp2
    version: 1
  name: sampleapp2
spec:
  containers:
  - image: nginx
    name: sampleapp2
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
